import type { ScanOptions, ScanResult } from './types.js';

export declare function scanAndCount(root: string, options: ScanOptions): ScanResult;
export declare function formatForCopy(root: string, options: ScanOptions): string;
